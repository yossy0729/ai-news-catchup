# 引き継ぎメモ（Claude → CODEX / 次の作業者）

## ★再開ポイント（2026-06-22 時点）

**情報設計の整理（2026-06-22）**: 「今日の動向」タブが速報(media)と一次情報(category)を同居させ鮮度ミスマッチ（速報=24h / 一次情報=最大30日）で分かりにくかったため、**タブを分離**。今は `今日の動向(速報のみ) / カテゴリ別(一次情報) / 公式動向 / 指標・ベンチ` の4タブ。カテゴリ別には「一次情報は毎日更新されない」旨を明記し、古さが未更新に見えないようにした。ティッカー/カードは色4色体系（🔵研究・モデル/🟢プロダクト/🟠インフラ・業務/🔴リスク・規制）＋色凡例に統一。速度は全ティッカー55px/秒に統一。SOTA指標の向きは▲▼。

**カテゴリ別タブを「地域軸→分野軸」に再編（2026-06-22）**: 旧タブは 全体/国内/海外/研究・モデル/規制・リスク/事業・基盤 と「地域」と「テーマ」が混在していた。テーマ軸に統一し、タブは `全分野 / モデル・研究 / 産業導入 / 規制・ガバナンス / セキュリティ / インフラ・基盤` の5分野（+全分野）。各分野は複数の収集カテゴリを束ねる（`app.js` の `FIELD_GROUPS`、cats が news.json/media-news.json の id と対応）。国内/海外はカード左の**地域バッジ**（`.region-badge` 国内=青灰 / 海外=薄紫、横断=バッジ無し）で表示。各分野は日付降順・最大4件、0件は折りたたみ。ステータス表記は 本日更新/最近の動向/未取得。



**SOTAウォッチを「全研究分野の自動収集」に刷新（2026-06-22・完了）**:
- データ源を **paperswithcode.co 公式API**（`/api/v1`）に変更。ブラウザUA付きfetchでJSON取得（WebFetchは403で不可）。`scripts/collect-sota.js` が全78分野を収集し `data/sota.json` を生成。
- 「データは全網羅・画面は厳選＋検索」方針。既定は `data/sota-presets.json` の前線10分野、タブでエリア別/全分野、`data/sota-labels.json` で日本語キーワード検索。
- 数値が取れるのは **55分野のみ**（PwC未登録の23分野はリンクのみ）。一部は更新停止で古いSOTA（PwCの構造的限界）。鮮度はプリセットで対処。
- 1位交代時のみ前回値を `prev*` へ退避（履歴蓄積）。`daily-update.js` に組込み済み・毎朝自動・失敗は隔離。
- 指標の向き(↑/↓)は1位2位スコアで自動判定。出典は Paper/Code/掲載元。`data/cache/` は .gitignore 済み。
- **公式上書き（2026-06-22 追加・候補①完了）**: `data/sota-official.json` で分野を公式ソース値に差し替え。`collect-sota.js` の `officialFetchers` に取得処理。現状3ソース:
  - `coding-agents` → SWE-bench Verified（swe-bench.github.io の leaderboards.json）
  - `automatic-speech-recognition` → Open ASR（huggingface/open_asr_leaderboard の en_shortform.csv）
  - `llm-arena`（新規・addIfMissing）→ LMArena Text（lmarena/arena-catalog の leaderboard-text.json の "full" カテゴリ）
  - `image-generation` → LMArena Image、`image-understanding` → LMArena Vision（同 arena-catalog）
  退避はベンチ/指標が同一のときだけ（ベンチ差し替え時は誤比較を避け履歴を持たない）。新ソースは fetcher を足すだけ。動きの速い注目分野に絞って上書きし、更新の遅い古典分野はPwCのまま。
- **代表ベンチ手動指定（2026-06-22 追加・候補②完了）**: `data/sota-datasets.json` で分野ごとに代表データセットを指定し、件数最多ルールを上書き。現状 世界知識=GPQA Diamond / 数学=AIME 2025 / 推論=BBH / 検索=BEIR。`collect-sota.js` の代表選定で dataset_name 部分一致を優先。指定が見つからなければ自動選定へフォールバック。
- **次の候補**: ③数学 AIME 2025 は GPT-5.2 が満点(100)で飽和ぎみ → AIME 2026 への切替を検討（現状データ少）。④取得できない定番ボード（Artificial Analysis=API登録制、MathArena=JS描画）は現状非対応。

---

## 旧・再開ポイント（2026-06-21 時点）

UI/機能はかなり進行。直近の完了：2タブ構成（今日の動向／指標・ベンチ）、固定コマンドバー（タブ＋検索）、
価格早見表（OpenAI/Anthropic/Google 全9モデル実値＋キャッシュ入力＋出力倍率＋ベンダーセル結合）、
SOTAウォッチ（ドメインタブ＋2行推移＋凡例）、毎日7時更新の表示＆タスク再登録。全コミット済み・ツリーきれい。

**④「直近24時間ニュースの鮮度＋空欄対策」= 完了（2026-06-21）**:
1. ソース棚卸し済み。固定・厳選プールに **ASCII.jp / Impress Watch / TechCrunch(AI)** を追加（計7フィード）。
   The Verge/Publickey は Atom形式で現パーサ非対応、ZDNETは404のため除外。
2. 鮮度を厳密化: `collect-media-news.js` に `--max-age-hours`(既定72h) を追加し古い記事を除外。
   `freshness` を `fresh`(<24h)/`recent`(24〜72h) に再定義＋`ageHours` を付与。
   app.js は `publishedAt` から相対表示（◯時間前/◯日前）を描画時に計算し、<24hには緑「新着」チップ。
3. 空欄対策: カテゴリに24h新着が無ければ直近72hで補完し「24時間以内の新着なし・直近72時間の注目を表示」を明示。
   ヘッダーは「24時間以内 N件 / 直近72時間 M件」表示に変更。
   ※プレビュー用に `.claude/launch.json` を追加（`node serve.js` を4173で起動）。

**次の候補タスク**: requirements.md §3.5 P1 **SOTA履歴の自動スナップショット**（週1〜月1で1位交代時のみ現在値→前回値へ退避）。


このファイルは、2026-06-20 に Claude Code が行った作業の引き継ぎ用です。
CODEX 再開時（2026-06-25 以降）や、将来の自分が読む前提でまとめています。

## 1. 作業場所が変わりました（重要）

- **旧**: `C:\Users\ritch\OneDrive\ドキュメント\AI News Catchup`（OneDrive配下）
- **新**: `C:\Users\ritch\Documents\Codex\2026-06-17\AI-News-Catchup`（OneDrive外）

OneDrive配下は Git の `.git` 同期競合・ファイルロックの原因になるため、OneDrive外へ移しました。
**CODEX 再開時は、この新しいパスを作業ディレクトリに設定してください。** 旧フォルダ（中身は空）は削除予定。

## 2. Git を開始しました

- 元は Git 初期化済み・コミット0件の状態でした。
- OneDrive外へ移したうえで **初回コミット（baseline）** を作成し、以降をバージョン管理しています。
- `.gitignore` で機密(.env)・ログ・スクショ・ブラウザ一時データを除外。
- 外部依存はゼロ（Node標準モジュールのみ）。`package.json` に起動スクリプトを記録。

## 3. Claude が行った変更（コミット単位）

| コミット | 内容 | 挙動への影響 |
|---|---|---|
| baseline | CODEX作業分のスナップショット | （基準点） |
| 死にコード削除 | `app.js` の重複 `renderTicker`（旧シグナル方式）と到達不能な `renderPriority` 本体を削除（計約242行） | なし（実行されないコードのみ除去） |
| パス対策 | `serve.js` の静的配信トラバーサル検査を厳密化（`root + path.sep`） | 正常動作は不変、不正パスを403 |
| HTML上限 | `serve.js` 外部HTML取得に5MB上限（`readCappedText`） | 巨大ページのメモリ保護 |

`git log --stat` で各差分を確認できます。

### 3.1 要件定義の確定と機能追加（2026-06-20 追記）

`requirements.md` v0 を作成し、主要な未決事項を決定（詳細は同ファイル §8）:
- 一次情報は折衷案C（メイン枠=聖域 / ティッカー=参考可）
- 重要度=5軸＋ハイブリッドLLMランキング（上位N件のみLLM再評価、キー無しはルールベース）
- RSS/API優先・HTMLは最終手段 / "検証済み"表現の廃止 / カテゴリ0件は折りたたみ

実装した機能:
- 「検証済み」文言の撤廃、0件カテゴリの折りたたみ
- **モデル価格早見表(P3)**: `data/pricing.json` 駆動。価格は誤値防止のため既定「要確認」＋出典リンク。
- **SOTAウォッチ(P1)**: `data/sota.json` 駆動。ドメインタブ(言語/画像/音声/マルチモーダル/コード)で
  全網羅を避ける。topModel/scoreは既定「要確認」＋出典リンク。

> **重要**: 価格・SOTAの数値は意図的に未記入(null=「要確認」)。公式pricing/リーダーボードで
> 確認のうえ `pricing.json` / `sota.json` を更新し `verified: true` にする運用（誤値で信頼を
> 損なわないため）。自動スクレイピングは公式がbot対策/JS描画で弾くため非推奨。

### 3.2 SOTAウォッチの発展（2026-06-21 追記）

- SOTA表は「実値＋出典リンクのハイブリッド」「推移の2行兼用表示(上=今回緑/下=前回灰)」に発展。
  実値はブラウザ拡張(Claude for Chrome)で公式リーダーボードを読んで記入（WebFetchは403で不可）。
  現在 SWE-bench Verified(swebench.com) と Open ASR(paperswithcode.co) が実値入り。
- 出典は Papers with Code の復活版 **paperswithcode.co**（旧.comはMeta終了）。
- 未実装の将来機能: **SOTA履歴の自動スナップショット**（週1〜月1で1位交代時のみ現在値→前回値へ退避）。
  requirements.md §3.5 P1 参照。

### 3.3 日次自動更新タスクの再登録（2026-06-21・重要）

OneDrive→新フォルダへの移動により、Windowsタスクスケジューラの「AI News Catchup Daily Update」が
**旧パスを指して毎朝失敗していた**（結果コード1）。`scripts/install-daily-task.ps1` を新フォルダで
実行して再登録済み（毎朝07:00・新パス）。**今後フォルダを移動したら必ず再登録すること。**

**取りこぼし対策（2026-06-22）**: 6/22朝にタスクが起動直後に中断（0xC000013A・スリープ/セッション要因）し当日分が更新されなかった。`install-daily-task.ps1` に `-WakeToRun`（スリープ解除実行）＋失敗時リトライ（10分間隔×3回）を追加して再登録済み。手動更新は `node scripts/daily-update.js`。

## 4. あえて見送った項目（将来の宿題）

ローカルMVPには過剰なため、意図的に未対応にしています。本番化・多人数化の段階で検討:

- **同期ファイルI/Oの非同期化**: 単一ユーザーのローカル利用では体感差ゼロ。改修リスクの方が大きいため見送り。
- **`serve.js`/`app.js` の重複ユーティリティ共通化**: サーバー用とブラウザ用で実行環境が異なり、共通化にはビルド導入が必要。「外部依存ゼロ・シンプル」の利点を壊すため見送り。

## 5. スケール時のコスト/セキュリティ注意（将来計画用）

このアプリは「1日1回の生成（LLM要約）でだけ課金」「閲覧は静的JSONを読むだけで無料」という構造。
読む人が増えてもコストはほぼ増えないが、**公開時は次の3点を必ず対応**:

1. `/api/update`（今すぐ取得）は認証なしで誰でも叩ける → 公開前に**認証＋レート制限**を付ける（連打で課金されるため）。
2. ユーザーごとのAI機能（チャット等）を全員開放する場合は**ログイン限定＋回数制限＋キャッシュ**。
3. APIキーは**サーバー側のみ**（フロントに出さない。現状は遵守）。

保険: OpenAIダッシュボードで**Usage limits（上限金額）と請求アラート**を設定。要約は安価モデル(mini/nano)維持。

## 6. 未レビュー領域

- `scripts/`（ニュース収集ロジック一式）は今回未レビュー。必要に応じて別途レビュー可能。

## 起動

```powershell
npm start          # = node serve.js  → http://localhost:4173/
npm run validate   # データ検証
```
